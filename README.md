# Power BI PBIP Dev/QA/Prod POC

This repo contains a Power BI Project (.pbip) with parameterized environment switching via pEnvironmentThis repo contains a Power BI Project (.pbip) with parameterized environment switching via pEnvironment and deployment pipeline rules.

## Structure
- /model  : semantic model (TMDL)
- /report : report metadata (PBIR/JSON)
